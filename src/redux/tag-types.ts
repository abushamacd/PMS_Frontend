export enum tagTypes {
  user = "user",
  profile = "profile",
  project = "project",
  section = "section",
}

export const tagTypesList = [
  tagTypes.user,
  tagTypes.profile,
  tagTypes.project,
  tagTypes.section,
];
